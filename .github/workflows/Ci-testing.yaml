name: my awesome web

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: List files and read README
        run: |
          echo "my first task"
          echo "Hello world this is simple GitHub Actions workflow!"
          ls -ltr
          cat README.md
